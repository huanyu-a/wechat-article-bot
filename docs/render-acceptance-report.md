# 组件渲染验收报告（结论页）

> 验收标准：**所有组件在后端 API 与编辑器前端两条路径都渲染正确。**
> 报告日期：2026-09-13（第十六~二十轮陆续更新，此后 **第二十二 ~ 第三十四轮**又逐轮追加，最后一轮 **2026-09-14**）
> 数据来源：第十二轮当轮重跑，**第十三轮照复现手册独立复跑一遍**，
> **第十四轮又把探针脚本纳入版本控制后整体重跑一遍**，**第十五轮用干净 clone 照手册实跑一遍**，
> **第十七~二十轮再各重跑一遍**（判定数字六轮完全一致）——结论不变。
> 另有第十四轮新增的「直接量应用实际提供的前端」复验（用于修您反馈的 43/44 故障），
> 以及**第十六轮**对您逐条标注的 11 条渲染缺陷的归属判定与修复（见第六节·补）、
> **第二十轮**对第 10 条定性的反向复核与降级（见第六节·补）、
> **第二十二轮**把上面这 11 条搬到**您真正看到的那个编辑器界面**上又量了一遍
> （结论：探针页与真实界面在**同宽下 70 组探针 0 组有差**，另 1 组只差图片加载时刻；
> 同宽后 11 条逐条实测见 `docs/dev/known-issues-handoff.md` §3.22）。
> **第二十三轮**把「您当时是怎么把这段内容弄进编辑器的」查清了（**打开文章这条路**，不是粘贴、也不是手打；
> 证据是**您自己那篇文章 #38 的存库正文**：8 条批注原话就写在正文里，且这些容器的外层 `<section>` 一层不少），
> 并把第 4 条从「只记录不修」**修掉**了（卡片凭空高出的 17px 归零，实测高 145 → 128，与产物 128 相同）。
> 详见 `docs/dev/known-issues-handoff.md` §3.23。
> **第二十五轮**把验收问题换成您的原话——「**我在编辑器里看到的样子，保存之后还在不在**」——
> 把拦截到的保存请求正文**原样灌回**编辑器、量同一组 11 条探针并与保存那一刻的画面逐条对照：
> **打开文章、粘贴两条路径上结论是「幂等」**（逐条相同、整段 DOM 重排声明顺序后逐字节相同）；
> 手打那条 3 处差异经收窄实验证明**发生在「打开时的解析」、不在保存**，保存出口本身仍是幂等的。
> 同时把 #38 那 4 条的症状**逐条定性**（1 条是旧编辑器保存时丢的、3 条是正文停在更早的渲染版本），
> 并给出存量影响面。详见 `docs/dev/known-issues-handoff.md` §3.26。
> **第二十六轮**把上一轮留下的**唯一一条「我们这边造成、您能看见」的差异修掉了**——
> 「段首用半角空格/制表符手工缩进，文章一打开就被合并」：改用只作用于**段首**的定点替换后，
> **段首空白存活 1/7 → 7/7**，而**79 个最小写法样例的块结构/位置/高度逐条不变（0 条不同）**；
> 同一轮还给「窄列被拍平」补了一道**带退出码的回归闸**（以后改回去会被自动抓住，不必再靠一次对照实验），
> 并把散在各处的「**需要您一句话**」的事项并成了**一张表**。详见 `docs/dev/known-issues-handoff.md` §3.27。
> **第二十七轮**做三件事：① 上一轮那处修复有一个**自认的小代价**（段首用制表符缩进时，宽度会从 26.89px 变成 3.38px）——
> 先把「这种写法到底有多少」量清楚（**产物 249 个 + 产物文件 403 个 + 存量文章 38 篇里，段首制表符 0 处**），
> 再把它**收掉**：制表符改成按制表位**等宽展开**，实测缩进位置 224.89 → **224.88px**（差 0.01px），
> 三条判据（空白存活 **7/7**、79 套样例 **0 条不同**、无新增视觉差异）全过；
> ② 把**「内容是怎么进编辑器的」三条路**量成一张表，顺带确认上一轮的修复把「粘贴纯文本」和「手打」两条路的
> **保存后再打开**也一并修好了（唯一没修的是「粘贴网页 HTML」，它**改前改后完全一样**，不是新问题）；
> ③ 产品代码变了就**全文重验**：#38 十一条四列表**改前/改后两列各 125 个值逐项全同**，
> 七套回归与第廿一轮快照一致（四份摘要逐字节相同）。详见 `docs/dev/known-issues-handoff.md` §3.28。
> **第二十八轮**做的不是修，是**立闸**与**补依据**：① 把「**打开 → 保存 → 再打开**」整圈钉成一道
> **带退出码的常规闸**（三条判据 ＋ 两个反例自检：当前代码 **exit 0**，修复前的整包前端 **exit 1**、恰好 4 处失败）；
> 这道闸顺带坐实了一个**方法论盲区**——只看「两次测量是否自洽」的判据，对「打开和再打开**一致地丢**」这类 bug
> **天然免疫**，抓得住它的是「**入口声明的量还在不在**」那条判据；
> ② 给您上一轮新增的第 9 条（**从网页复制带缩进的段落粘进来，缩进会没有**）补齐了「**要不要修**」的完整依据，
> **结论是建议不修**：7 种缩进写法里，真复制出来的载荷**只有 1 份**会被修法改动，而那一份**本来就不丢**
> （浏览器复制时会把 `white-space: pre-wrap` 一起写进剪贴板，ProseMirror 认这条声明、原样保留空白）；
> 把候选修法施在 249 个产物上只有 4 个文件 / 8 处被改写，**命中表格/代码块/公式/SVG 的 0 处**——
> 即**收益为 0，代价是去动一条本来没坏的全局路径**（粘贴 ＋ 拖动）。**最终修不修，仍等您一句话。**
> ③ **本轮没有改任何一行产品代码**；七套回归重跑与第廿一轮快照逐项一致，#24/#38 全程只读。
> 详见 `docs/dev/known-issues-handoff.md` §3.29。
> **第二十九 ~ 第三十三轮**没有新增产品改动，做的是**把「绿灯」本身变成可核查的**：
> 全库 21 条验收断言逐条分类并真喂坏输入（第二十九轮）、把九步链 14 步缺失的退出码补齐（第三十轮），
> 此后每轮都对上一轮的改动做**回归复查**，并把发现的每一处口径问题如实记下而不是就地抹平
> （第三十一 ~ 三十三轮；其中一条要记住的：**退出码非零 ≠ 判据判红，引「某一步非零」之前先看有没有 `[闸]` 行**）。
> 详见 `docs/dev/known-issues-handoff.md` §3.30–§3.34。
> **第三十四轮**修掉您新点名的第 9 条表格问题（**表格应自适应高度与宽度，留白不要太大，可与原项目对比**）：
> 根因是本项目给「手写表格」准备的兜底样式（等宽列、每格 1px 网格线、行距 1.95 倍）**把渲染服务写下的表格排版整片盖住**。
> 修法只做两件事——打开正文时把产物表格的样式原样贴回并打一个标记类，兜底那三条改成**只对没有这个类的表格生效**。
> 实测：表高 **347.09 → 212**（原项目 **197**）、列宽从四等分回到接近原项目的比例、网格线从「四边都有」变回
> **与原项目逐字符相同**的「只有下边」；余下 15px 经六面量法归因到**字体行盒**（换成原项目那串字体即降到 197），
> 不是表格样式能救的那部分。11 个产物全量扫描 **PASS 11 · WARN 0 · FAIL 0**；
> 并且专立一支**反作用闸**证明**手写表格没被误伤**（工具栏插一张表，重开后仍走原来的兜底样式、网格线不变）。
> 详见 `docs/dev/known-issues-handoff.md` §3.35。
> 面向读者：不需要懂代码。看完**第〇节**即可判断「现在能不能验收」，细节在下面各节。

---

## 〇、终稿状态速览（一屏读完）

**① 覆盖面**：组件总数 **63**，后端与编辑器**两条路径都正确 25**，**渲染引擎未实现 38**（全是 `layout-*` 一族），**悬空 0**。

**② 您点名的 11 条，逐条已有最终裁定**：

| 状态 | 条数 | 是哪几条 |
| --- | --- | --- |
| **我们这边已修** | **8** | 1 · 3 · 4 · 5 · 6 · 7 · 8 · 11 |
| 我们这边只记录不修 | **0** | —（第 4 条已在第二十三轮修掉：编辑器凭空给它多包的那层段落加了 17px 间距，现已归零） |
| **上游渲染缺陷**（§R5–§R9） | **4** | 2 · 5 · 6 · 9（已整理成可直接转发的清单） |
| **驳回**（查无此缺陷） | **2** | 3（圆形裁切是源图横版的设计行为）· 10（原写法复现不出） |

> 第 3、5、6、9 条同时有两侧成因，所以上表**不是互斥分区**。逐条原话、DOM 证据与截图见**第六节·补**。

**③ 还差两件事才能结案**：ⅰ **第 10 条**是唯一一条「您报了、我们复现不出」的——请把当时出错时的原文或截图发我们；
ⅱ 上游把 §R5–§R9 这 5 个条目处理掉（我们改不动，但已给出可用的替代写法，**不影响您现在使用**）。
另外，**所有「需要您一句话」的事项（含工作树要不要提交、重启后前端可能仍是旧包、存量两篇要不要重渲染等）
第二十六轮并成了单一列表**：`docs/dev/known-issues-handoff.md` **§3.27③**，按「建议优先做 / 可缓 / 只记录」分组。

**④ 补一句第二十四~二十五轮在您自己那篇文章（#38）上的复验结果**（详见第六节·补，两种口径别混起来读）：
把验收搬回 #38 逐条重量后，**8 条批注里 5 条的症状确实消失**（第 4、7、8、9、11 条），
**4 条仍在**（第 1、5、6、10 条）——这 4 条**不是修复没生效**，是那篇文章存下来的正文本身跟今天的新鲜渲染不一样；
#38 又没留下 Markdown 源文、无法重渲染。
**第二十五轮把这 4 条逐条定性了（不是一句「停在修复前」带过）**：
**第 6 条（列宽）是旧编辑器在保存那一刻丢的**（同题对照实测：改前出口两列都是 `min-width: 25px`，
改后出口保留 `width: 90px`，与 #38 存库正文的指纹一致）；
**第 1、5、10 条是正文停在更早的渲染版本**（同一份输入喂改前编辑器，外层卡片的边框/圆角/底色**一个不少**，
所以不是保存丢的；#38 存的片段与今天的产物**连文字都不一样**）。
把这几条的**新鲜渲染结果**灌进同一个编辑器，量出来与后端产物**逐项相同**。
**⑤ 所以「您看到的和保存下去的是不是同一份」——现在是实测过的**：
打开文章、粘贴两条路径**逐条相同**（这条可以直接写死进验收）；
手打那条原本唯一会掉的写法（**段首用半角空格/制表符手工缩进**）**第二十六轮已经修掉**：
段首空白**存活 1/7 → 7/7**，且 79 个最小写法样例的块结构/位置/高度**逐条不变（0 条不同）**。
`&nbsp;`、全角空格、零宽空格本来就不会丢。
> 代价只有一处，已实测：**用制表符**做段首缩进时，缩进宽度 26.89px → 3.38px（半角空格与 `&nbsp;` 宽度不变）。
> **第二十七轮已把这处代价收掉**（改成按制表位等宽展开，实测 224.89 → **224.88px**），**不再需要您拍板**；
> 同一轮还把「打开 / 粘贴 / 手打」四条输入路径量成一张表（见 §3.28）。**修复目前只在工作树里，尚未提交。**

---

## 一、一句话结论

> **能验收。** 渲染引擎现有的 **63 个组件**里，**25 个**在后端与编辑器两条路径上都渲染正确；
> 剩余 **38 个**是**渲染引擎本身没有实现**——不是本项目的编辑器画坏了。
> 对这 38 个，本项目已经做到「禁止 AI 写这类写法 + 保存时给出改写提示 + 给出能用的替代写法」。
> （措辞更正见第五节第 1 条：这道理是**提示**不是**拒绝**，保存仍然成功。）
> **没有任何一个组件处于「说不清楚」的状态**（下面表格里的「悬空」一栏为 **0**）。

---

## 二、总数与分类

| 项目 | 数量 | 说明 |
| --- | --- | --- |
| 组件总数 | **63** | 出自渲染引擎自带的「支持哪些组件」清单，是组件全集的唯一权威出处 |
| **两条路径都正确** | **25** | 后端渲染出来的结果对，送进编辑器后再存一遍也还是对的（其中 13 个还在真实文章里验证过） |
| 不能渲染 | **38** | 原因见下 |
| **悬空（说不清）** | **0** | 每一个都有实测证据 |

> 四个数字连起来读就是：**组件总数 63 / 两条路径都正确 25 / 引擎没实现 38 / 说不清的 0**。
> （内部沟通里这条常被简写成「组件总数 63 / 两路径均通过 25 / 等上游 38 / 悬空 0」，
> 两种说法指的是同一组数字。）
>
> **第三十四轮补一句**：上面这四个数字是**组件级**的覆盖统计，此后几轮修的是**已覆盖组件在编辑器里的排版保真**
> ——同属「两条路径都正确」那 25 个的组件，此前仍然可能长得和原项目不一样（表格就是最典型的一个：
> 列宽被拍平、行距偏大、多了三条网格线）。它**不改变** 63 / 25 / 38 / 0 这组数字，只改变「正确」二字的成色。

### 38 个不能渲染的原因归类

| 原因 | 数量 | 是不是本项目的问题 |
| --- | --- | --- |
| **渲染引擎没有实现** | **38** | **不是。** 这 38 个同属一族「版式组件」（名字都以 `layout-` 开头）。实测 38 个名字 × 2 种写法 = **76 种写法**，逐种调用真实渲染接口：**76 种全部把语法原样留在结果里**（接口不报错、也不给任何警告，等于静默地不认识）。引擎代码本身也印证这一点：这 38 个名字在引擎里**只是普通字符串，没有任何一处用来识别语法、也没有任何一处用来渲染** |
| 待重启落库 | **0** | 曾有一项**不是组件**的待办（给 AI 的写作说明文案没更新到数据库）。**第十四轮重启后已落库、已关闭**，见第五节第 3 条 |
| 其他 | **0** | — |

> 通俗说法：这 38 个不是「我们的编辑器画不出来」，而是「**引擎本身就不认识它们**」。
> 后端和编辑器都没有画错——两边都忠实地把这段引擎没认出来的文字原样保留了。

---

## 三、用户点名的两个组件

### 3.1 公式 ✅ 两条路径都正确

- **真实文章**：第 **43** 篇「[probe] 第七轮 公式端到端」（数据库里的真实文章，不是测试样例）
- **后端 API**：HTTP 200，渲染成功；源文 166 字符 → 产物 10266 字符，正文里没有残留未识别的标记
- **编辑器前端（真实 Chrome 打开真实页面实测）**：
  - 公式元素 **5 个**（3 个行内 + 2 个独立成行的块级），**全部可见**
  - 实测高度 **[22, 22, 22, 45, 53]** 像素 —— 都不是 0，说明**真的排成了公式**，而不是显示成一行普通文字
  - 两个块级公式的高度是 **[45, 53]** 像素
  - **没有露出源码**：正文里看不到 `$$` 或 `\frac` 这类标记
  - 公式专用字体：**已加载**
- **截图**：`target/probe/browser/shots/articles/43.png`
- **原始数据**：`target/probe/browser/articles_result.json` → id=43

### 3.2 轮播图 ✅ 两条路径都正确

- **真实文章**：第 **44** 篇「[probe] 第七轮 轮播端到端」
- **后端 API**：HTTP 200，渲染成功；源文 196 字符 → 产物 1437 字符，正文明细里含 **1 个矢量图容器**，没有残留未识别的标记
- **编辑器前端（真实 Chrome 打开真实页面实测）**：
  - 轮播容器 **1 个**，**可见**
  - 实测尺寸 **600 × 200** 像素，与写作时声明的尺寸完全一致
  - 含 **1 个动画元素**（证明是**真的会轮播**，不是一张不动的占位图）
  - 含 **3 个分帧容器**（轮播图分帧结构的证据）
  - 内嵌 **3 张图片全部加载成功**（3/3），实际显示尺寸 600×436，图片原始分辨率 1080×784 —— **有真实画面，不是空框**
- **截图**：`target/probe/browser/shots/articles/44.png`
- **原始数据**：`target/probe/browser/articles_result.json` → id=44

> 补充：公式与轮播还各自有「最小写法」的浏览器实测，判定同样是**通过**，
> 截图见 `target/probe/browser/shots/all/math-inline.png`、`math-block.png`、`ctn-slider.png`。

---

## 四、这 25 个「两条路径都正确」的组件是谁

| 证据强度 | 数量 | 含义 |
| --- | --- | --- |
| **A 级** | **13** | 真实文章里真的写过这个组件，**并且那篇文章在真实编辑器里回归过**——最强的证据 |
| B 级 | 12 | 用最小写法样例在**真实 Chrome** 里实测过。仍是真实浏览器，不是软件模拟 |
| C 级 | **0** | 「只靠软件模拟浏览器（不开真浏览器）得出结论」的组件——本报告**一条都没有** |

**A 级 13 个**（括号里是出现过它的真实文章编号）：
`<p-title>`(11,12,30,35,37,40)、`<lead>`(12,16,37,40)、`<engage-label>`(30,35)、
`<engage>`(12,30,35,37,40)、`<statement>`(11,12,35,37,40)、`<img>`/Markdown 图片(11,13,16,35,37,40)、
`<badges>`(3)、`:::reading-path`(30,35)、`:::breaking`(30,35,40)、`:::timeline`(30,37,40)、
**`:::slider` 轮播(44)**、`:::callout`(3,16,30,35,37,40)、`:::table`(30,37,40)。

**B 级 12 个**：`<title>`（对应引擎清单里的两个 ID：`Title_DA01`、`Title_DA02`）、`<cta>`、`<Badge/>`、`<Icon/>`、
`:::steps-horizontal`、`:::steps-vertical`、`:::case-flow`、`<gov-header>`、`:::code-block`、`:::hint`、`:::align`。

---

## 五、已知未覆盖 / 已知局限（不只写好消息）

1. **那 38 个「版式组件」在本项目里渲染不出来**——因为引擎没实现。
   本项目的应对是三道防线（写作说明里禁用、保存时给出改写提示、给出替代写法），
   已实测 **10 条替代写法在两条路径上 100% 通过**（后端 10/10、编辑器 10/10）。

   > **措辞更正（第十五轮复核）**：此前本节写的是「保存时**拦截**」，不准确。
   > 实际实现是**提示**：AI 保存草稿时会拿到一份改写建议（写在它一定会读到的工具返回值里），
   > 同时应用日志留一条 WARN；**保存本身仍然成功，不会拒绝**。
   > 而且这条防线只覆盖**智能体写的草稿**——编辑器里人工输入并保存的路径**没有**这项检查。
   > 另经全族逐名核验：这 38 个名字 × 容器/标签两种写法共 76 组，**每一组都会被提示命中**
   > （不是抽样；对照组 19 个受支持容器零误报）。
   **如果哪天引擎实现了它们，这份结论需要重跑一次**——当前结论只对「现在这个引擎版本」成立。
2. **A 级证据只覆盖 63 个里的 13 个**，其余 50 个停在 B 级。
   这不是缺陷（B 级已经是真实浏览器实拍），但「真实成文里用过它」这一层还没到，
   要靠后续实际写稿真的用到这些组件来补。
3. ~~**给 AI 的写作说明文案，数据库里还是旧版**（旧版 5635 字符，源码已经是 6649 字符）。~~
   **已于 2026-09-13 晚重启应用时落库**：库里实测已变成 **6649 字符**、与源码一致（`UPDATED_AT` 为本次重启时刻）。
   **这一项已经关掉，不需要用户再做任何事。** 保留在此仅为说明它曾经是唯一需要用户动手的事项。
   渲染结果本来就不受它影响（写了禁写写法时会收到改写提示）。
4. **删除过的文章，打开时界面会一直转圈**（一个已知的旧问题，截图 `shots/articles/5.png`）——
   与本次验收标准无关，但要如实说明它还在。
5. **公式和轮播的 A 级证据各只有 1 篇文章**（第 43、44 篇，是本项目为验证而生成的稿件）。
   样本量小是事实；但两条路径用的都是真实接口 + 真实数据库 + 真实浏览器，不是软件模拟。
6. **「能验收」的范围要说清**：本报告证明的是「引擎支持的那 25 个组件，两条路径都渲染正确，
   且 38 个不支持的已查清原因并给出可用替代」。它**不**代表「任何写法在任何情况下都渲染正确」——
   组合写法（嵌套、重复、超长、混排）另有 17 个用例单独测过，结果是编辑器侧 17/17 通过，
   差异全部出在引擎侧。
7. **真实稿件里用到的写法，编辑器侧 39/39 全通过**（第十五轮补的证据）：
   全库 44 篇稿件扫描出 39 种被实际用到的写法，每一种都有「最小写法 + 真实浏览器」的通过判定。
   其中 **7 种**（链接、图片、`<title>`、行内高亮、案例流、横向/纵向步骤）**没有**在真实成稿的编辑器里打开过，
   证据强度停在 B 级——这是如实说明，不是缺陷。
8. **有一个会复发的故障，本轮只查清了、没有修**（第十五轮确认）：
   应用启动**不会**自动构建前端界面。如果先执行过整体清理（`mvn clean`）再启动，
   应用起来后**界面是空的**（这正是您此前反馈「43 公式、44 轮播不显示」的同一类问题）。
   已实测：普通重启**不会**退回旧界面（重启前后 39 个前端文件逐字节不变）；只有清理过才会。
   修复属于构建结构调整，**需要您拍板**，方案已列在 `docs/dev/render-verification.md` §3.11④。
9. ~~**「段首用半角空格手工缩进」这种写法，打开文章时会被合并掉**~~ → **第二十六轮已修，这条不再是缺陷**。
   半角空格和制表符在打开时会被并掉（`&nbsp;`、全角空格、零宽空格不会）。第二十六轮改用**定点替换**
   （只把段首那段空白换成不会合并的 `&nbsp;`），实测：段首空白**存活 1/7 → 7/7**，
   而**79 个最小写法样例的块结构、位置、高度逐条不变（0 条不同）**。
   **唯一需要您知道的代价**：用**制表符**做段首缩进的写法，缩进宽度会从 26.89px 变成 3.38px
   （半角空格与 `&nbsp;` 的宽度完全不变，都是 3.38px）。
   > **第二十七轮已把这处代价收掉，不需要您拍板了**：先量出这种写法在产物与库存文章里**一处都没有**，
   > 再把制表符改成**按制表位等宽展开**——实测缩进位置 **224.89 → 224.88px**（差 0.01px），
   > 三条判据（空白存活 7/7、79 套样例 0 条不同、无新增视觉差异）全过。见 §3.28①。
   > 上一轮评估过的那条候选修法（打开「保留全部空白」）**依然不能用**——它会改掉整篇块结构
   > （顶层块 14 → 15、块位置整体位移、有的块高从 280.69 变成 87.75）。本轮用的是窄修法，**没有付这个代价**。
   > 判据与逐条实测见 `docs/dev/known-issues-handoff.md` §3.27① 与 §3.28①。
   > **第三十五轮已随其余改动一起提交**（见下方第 8 条的同一张表）。
   >
   > **第三十五轮把这处修法的射程从「打开」扩到了「粘贴」**（`PastedLeadingWhitespace` 扩展的
   > `transformPastedHTML`）——此前「从网页复制一段 HTML 粘进来」这条路上，段首空白仍然在**粘的那一刻**就丢。
   > 同一份载荷、同一支脚本、同一台浏览器，**唯一自变量是前端包**，实测：
   > 旧包实时缩进 `[0, 0, 0]`、保存出口里一个 `&nbsp;` 都没有；新包 `[6.72, 26.88, 0]`、
   > 保存出口 `<p>&nbsp;&nbsp;LEAD-SP</p>…`、**再打开还是 `[6.72, 26.88, 0]`**（进得去、存得住、再打开还在）。
   > **另外四条输入路径（打开文章 / 粘贴纯文本 / 手打 / 修好的粘贴）逐字节未变**，说明射程没有溢出；
   > 并且改的只是**挂载点**，第二十六轮那个真函数**一个字节都没动**（切块 sha 与第二十八轮记录逐字相同）。
   > 新立的审计器 `tools/render-verify/browser/r35-paste-fix-audit.mjs` 自带反例自检（旧包必须判红），
   > 实测 **EXIT=0**，依据见 `docs/dev/known-issues-handoff.md` §3.36⑤、`docs/dev/render-verification.md` U19。
   > ⚠️ **第二十八轮「建议不修」这个结论本身没有被推翻**：当时判的是**收益**（7 种真复制载荷里 1 份会被改写、
   > 249 个产物改写 4 文件 8 处），本轮判的是**「坏在哪 + 修了会不会坏别的」**——换了判据，数字继续有效。
10. **库里有 2 篇文章（占 5.3%）的正文带着旧编辑器留下的痕迹**（#24、#38，第二十五轮只读扫描）：
    其中「窄列被拍平」那一条两篇都有，另外 3 条只在 #38。
    **现有的「重新渲染」功能救不了它们**——那个功能要求文章留有 Markdown 源文，而这两篇都没有。
    影响面、判据（您可以自己重跑）与方案见 `docs/dev/known-issues-handoff.md` §3.26③；
    **要不要动这两篇的生产数据，等您定，我们不替您决定。**
11. **所有「需要您一句话」的事，第二十六轮并成了一张表**（现象 / 影响面 / 需要您点头的动作 / 不修的后果），
    按「建议优先做 / 可缓 / 只记录」三组排好：`docs/dev/known-issues-handoff.md` **§3.27③**。
    其中在第 8、9 条之外还包括：工作树里的改动要不要提交、`:::infographic` 当时的具体写法、
    列宽入口的一个已记录小缺口、以及几处**需要您点头才敢删**的临时文件。
    **第二十七轮没有推进其中任何一条**，只做了两处更新：把「制表符缩进宽度」那条（第 4 条）标成**已收掉、无需拍板**，
    并**新增第 9 条**——「粘贴网页 HTML 进来的内容，段首缩进在粘的那一刻就丢」（改前改后完全一样，不是新问题；
    要修是一行，但它会改「粘贴」这条路的全局行为，**要您点头才动**）。
    **第二十八轮同样没有推进任何一条**，只做了一处**事实订正**（第 1 条里那串「工作树有多少改动」的统计
    是第二十六轮写的，到本轮已经过期，已按本轮实测改成 46 行 / 已跟踪 13 个文件 / 未跟踪 33 个；
    **要不要提交这件事本身没有动**），并给第 9 条**补上了完整依据**——
    结论是**建议不修**，理由与数字见 `docs/dev/known-issues-handoff.md` §3.29②。**修不修仍等您一句话。**
12. **一条口径更正（第三十轮加，只加注记、不重写历史）：此前往本报告汇报的「九步链每一步 `EXIT=0`」，
    当时的真实含义比字面窄。** 第二十九轮的全库断言审计（`tools/render-verify/round29_gate_audit.mjs`）
    实测：九步链 15 个步骤里**只有 1/9 那次前端构建真正带退出码**，其余 **14 步（2/9 ~ 9g）的 `EXIT=0`
    只表示「脚本没抛异常」，不含任何通过/不通过的判据**——某一步哪怕该判红，它照样打印 0。
    **这不是说那些数字是假的**：每一步跑出来的用例数、pass/na/fail 分布、截图、逐条差异表都是真的，
    本报告第二～四节的结论也确实建立在它们之上（且第十三、十五轮各独立复跑过一遍）。
    要收窄的只是**「`EXIT=0` ⇒ 这一步通过」这个推论**——在第二十九轮之前，它不成立。
    **第三十轮已把这 14 步的退出码补齐**（判定口径一个字没改，只是把脚本本来就在打印的 fail 数接到退出码上），
    并逐支喂了「已知有问题的输入」验证它**真的会判红**；从第三十轮起，「九步全绿」才可以按字面读。
    喂了什么坏输入、退出码是多少，见 `docs/dev/known-issues-handoff.md` §3.31① 的表。
    **第三十一轮对上面这段做了一次回归复查**：14 步逐支重喂坏输入（全部非零退出且判词可读）、
    对当前代码重跑（全部 0，无误报），九步链端到端重跑 16 步 15 绿、6/9 仍是真红（重新核过：13 张外链图 13/13 返回 404，
    同一时刻站点根仍 200）。**复查没有推翻上面任何一句**，只补了两处工程细节与一处审计器的「假绿」修补，
    见 §3.32；其中 **9e 还剩一处已量明的结构性盲区**（「纯表 B」的 40 条样例没有独立产物认领，
    「清单与实测一起少」时判不出来），本轮**只记录、未改判据**——要不要堵留给下一轮或您拍板。
    **第三十二轮把「没有输入时会怎样」这件事推到了全体 16 步**，结论大多是好消息、有两处不是：
    逐支移走前置产物，**14 支如实判红**（都走 `ENOENT` 或显式 `dist 不存在`）；
    把产物截成 0 条 / 1 条（分母变小）时，9a/9b/9c/9d/9e/9g **全部判红**——其中 9a~9d 新加的「分母锚」
    经**反事实对照**证明是它给的红（剥掉那一句，六例全部由红转绿）。
    仍**静默绿**的两处：**9f 缺 `round11_live_bundle.js` 时 `EXIT=0`，「通道 B2」整节从产物里消失**
    （88 行 → 75 行，`summary` 字段一个不缺，跑完整条链仍是绿的），以及 **E2 自检在「用例少一条」时不判红**；
    另有 `cdp.mjs` 的浏览器清理链 3 处与 `run-suite.sh` 收尾打印 1 处在失败时静默——全部**保留证据、未改判据掩盖**，
    要不要堵写成待拍板项（**都不需要新增输入文件、不改仓库结构**）。同轮实测 `run-suite.sh` 自身：
    子步骤被 SIGTERM/timeout 打死时分别记 **143 / 124**（都非零），某一步脚本文件不存在时**不停、继续跑完、最终 `EXIT=1`**。
    明细见 `docs/dev/known-issues-handoff.md` §3.33。
    **第三十三轮对上面这一段做了回归复查**：三类锚（分母锚 / 缺前置 / 退化产物）逐项复跑，**结果与第三十二轮逐行相同**
    （反事实 6/6 仍是「现状红、剥掉锚转绿」），所以那些红确实来自锚、不是别处顺手红的；
    9e 补做了第三十二轮没做的两个方向与反事实，并把 **D3 的盲区坐实**（两边一起少同一条时仍判绿）。
    两处**订正**：① 9e 那条链在「产物比清单少」时会以 `TypeError` **崩掉**而不是判红（退出码非零但没有任何判词），
    第三十二轮 A2 表里 D-5 那一格「判红 ✅」应读作「崩溃」；② 9f 的「`live_bundle` 已放回并重生成产物」
    **经复核是真恢复**（sha256 与文档所记一致、mtime 未变、内容是 38 个字面量的真构建产物、重跑可复现），
    但 D1 那个「缺它就静默绿」的缺口**依旧在**。**本节此前写下的结论一句都没有被推翻**，
    收窄的只是「某一步退出码非零 ⇒ 那一步判红了」这个推论——引用时先看有没有 `[闸]` 行。明细见同文件 §3.34。

---

## 六、需要用户做什么

### 现在就可以验收

上面第二～四节的结论，都在同一轮里**完整重跑**过，并且**第十三轮照复现手册又独立复跑了一遍**，
**第十五轮再用一个全新 clone（只含版本控制内容）照手册实跑一遍，结论依然不变**：
后端全量测试 **308 项、0 失败**；编辑器构建**成功**；
79 个最小写法样例在真实浏览器里 **70 通过 / 9 不适用 / 0 失败**；
17 个组合用例**编辑器侧 17/17 通过**；
38 个版式组件的 **76 种写法**逐一实测，另有独立交叉验证 **18/18 确认、0 存疑**。
（也就是说：**这份报告不是只有原始那台机器能复现**——任何人都能照手册跑出同样的数字。）

### 之前唯一需要您做的事，已经做完了

> 第五节第 3 条那条「数据库里的写作说明还是旧版」，在 **2026-09-13 晚重启应用时已经自动落库**：
> 库里实测 **6649 字符**，与源码一致；五个关键片段都能检索到。
>
> **您现在不需要再做任何操作。** 本报告的全部结论即时可用。

### 顺带修掉的一个真实故障（2026-09-13 晚）

您反馈的「`/articles/43` 公式不显示、`/articles/44` 轮播不显示」——**已定位并修复**，根因不是渲染能力：

- 应用对外提供的前端仍是**几天前构建的那一份**（编辑器代码里还没有公式与矢量图的保真处理），
  而当时验证用的前端已经是最新构建。**两边不是同一份文件**，所以出现「验证全绿、您打开是坏的」。
- 处理：把最新前端构建进应用实际读取的目录并重启应用。
- 复验（真实浏览器直接打开 `http://127.0.0.1:8081`，不是本地临时页面）：
  第 43 篇公式 **5 个全部可见**、高度非零、无源码残留；第 44 篇轮播 **1 个可见、600×200、3 张图全部加载成功**。
- 防复发：复现手册新增了一支「直接量应用实际提供的前端」的套件（原套件量的是构建目录，
  看不到这类不一致），并写进了已知坑清单。

---

## 六·补、您在编辑器里逐条标注的 11 条（2026-09-13 第十六轮）

您把 11 条渲染问题连同原话一起标了出来。这一轮的做法是：**每一段源码原样**（不改一个字）
打一次真实渲染接口，再把产物灌进真实编辑器量一遍，然后逐条判定「这条是渲染服务的，还是我们编辑器的」。

**结论：11 条逐条都有了最终裁定，数字是「8 + 0 + 4 + 2」**——**编辑器侧 8 条**（**8 条已修**：
第 1、3、4、5、6、7、8、11 条；**0 条只记录不修**，第 4 条已在第二十三轮补修，见
`docs/dev/known-issues-handoff.md` §3.23②），**上游侧 4 条**（第 2、5、6、9 条，
对应上游清单 **§R5–§R9** 共 5 个条目），**驳回 2 条**（第 3 条「头像没渲染完整」= 源图本身是横版的设计行为；
第 10 条 = 原写法复现不出）。**第 3、5、6、9 条同时有上游与编辑器两侧成因，所以这四类不是互斥分区**，
逐条明细见下表。

| # | 组件 | 您说的 | 判定 | 现在怎么样了 |
|---|---|---|---|---|
| 1 | changelog | 缺少边框 | 我们这边 | **已修**。边框本身在（截图为证），丢的是条目胶囊的排列方式 |
| 2 | subscribe | 原项目就没有正确显示 | **渲染服务** | 等上游。渲染结果里输入框和按钮是**假的**（不是真的表单元素） |
| 3 | author-card | 头像没有渲染完整 | 渲染服务（源图）+ 我们 | 编辑器侧**已修**。圆形裁切是设计行为，源头那张图本身是 1080×784 的横版 |
| 4 | quote-card | 与原项目不一样，缺少底色 | 我们这边 | **已修**（第二十三轮）。底色**本来就没丢**（渐变实测 1→1）；差的是编辑器**凭空多包的那层段落**带着 17px 下间距、把卡片撑高。现已归零：卡片实测高 **145 → 128**，与产物 **128** 相同 |
| 5 | audience-fit | 原项目就没有正确显示 | **渲染服务** + 我们 | 编辑器侧**已修**。渲染结果把您写的第三列（评级）**丢掉了**，但评级其实被用来决定徽标颜色 |
| 6 | title DA01 | 没有原项目的好看 | **渲染服务** ×2 + 我们 | 编辑器侧列宽**已修**。上游两处：卡片的**阴影属性名被吃掉**（所以没有投影）、字数**恒显示「共 0 字」** |
| 7 | summary | 每一项的前边缺少列表符号 | 我们这边 | **已修**。圆点现在真的画出来了（截图 `shots/r16/zoom/r16-07-summary.after.png`） |
| 8 | checklist | 每一项的前边都多了一条竖线 | 我们这边 | **已修**。那条「竖线」其实是 20×20 的方框被压扁成一条线，现在恢复成方框 |
| 9 | table style="card" | 行间距太大、整个表太高 | **渲染服务**（标题） | 逐行高度的差异从 **6 条压到 2 条**，剩下 2 条**都不是我们这边的问题**：1 条是表头取样的位置错位，另 1 条（第 3 行高 48.34 → 73.69）经双向复验证明是**量具两栏宽度不等**造成的多折一行——把两栏宽度对齐后，两边的行高一模一样。上游那边 `title=` 这个属性被丢了 |
| 10 | infographic | 原项目就没有正确显示 | **复现不出您这一条**（写法本身没问题）；旁边的写法有文档缺口，已记上游（§R10，P3） | **您给的写法本身是完全正确的**（`body: |` + 缩进，能正常渲染，我们复现不出任何缺陷）。旁边的写法（少写一个 `\|`、或写成条目列表）会让**三条正文整块消失**且不报错——但第二十轮反向自查证明**那是写法不合引擎的块标量约定，不是渲染缺陷**，所以我们把 §R10 **降级为「官方文档没写这条规则 + 静默失败」**，不再主张上游渲染有 bug。**您当时用的具体写法还想跟您确认一下**（您给的这段是好的，所以还不能说您看到的就对应这一条） |
| 11 | steps-horizontal | 每一步的边框没有加圆角 | 我们这边 | **已修**。「圆角画不出来」的根因是表格的三条排版声明没进编辑器，现在补上了 |

**逐条留痕索引（每条都能自己查到裁定与证据）**——产物目录 `target/probe/`（每次可重算，不入库）：

| # | 后端产物（渲染服务返回的原文） | 编辑器实时 DOM 证据 | 逐属性差异明细 | 1:1 对照截图 |
| --- | --- | --- | --- | --- |
| 1 | `r16/r16-01-changelog.html` | `browser/r16_result.json` → `r16-01-changelog`（5 组探针） | `r16/r16_summary.md` 第 1 条（21 条差异） | 有 |
| 2 | `r16/r16-02-subscribe.html` | `browser/r16_result.json` → `r16-02-subscribe`（6 组探针） | `r16/r16_summary.md` 第 2 条（0 条差异） | — |
| 3 | `r16/r16-03-author-card.html` | `browser/r16_result.json` → `r16-03-author-card`（5 组探针） | `r16/r16_summary.md` 第 3 条（5 条差异） | — |
| 4 | `r16/r16-04-quote-card.html` | `browser/r16_result.json` → `r16-04-quote-card`（4 组探针） | `r16/r16_summary.md` 第 4 条（15 条差异） | — |
| 5 | `r16/r16-05-audience-fit.html` | `browser/r16_result.json` → `r16-05-audience-fit`（4 组探针） | `r16/r16_summary.md` 第 5 条（6 条差异） | — |
| 6 | `r16/r16-06-title-da01.html` | `browser/r16_result.json` → `r16-06-title-da01`（11 组探针） | `r16/r16_summary.md` 第 6 条（8 条差异） | — |
| 7 | `r16/r16-07-summary.html` | `browser/r16_result.json` → `r16-07-summary`（7 组探针） | `r16/r16_summary.md` 第 7 条（30 条差异） | 有 |
| 8 | `r16/r16-08-checklist.html` | `browser/r16_result.json` → `r16-08-checklist`（6 组探针） | `r16/r16_summary.md` 第 8 条（27 条差异） | 有 |
| 9 | `r16/r16-09-table-card.html` | `browser/r16_result.json` → `r16-09-table-card`（8 组探针） | `r16/r16_summary.md` 第 9 条（5 条差异） | — |
| 10 | `r16/r16-10-infographic.html` | `browser/r16_result.json` → `r16-10-infographic`（6 组探针） | `r16/r16_summary.md` 第 10 条（28 条差异） | — |
| 11 | `r16/r16-11-steps-horizontal.html` | `browser/r16_result.json` → `r16-11-steps-horizontal`（8 组探针） | `r16/r16_summary.md` 第 11 条（7 条差异） | 有 |

> 1:1 对照截图只有第 1、7、8、11 条（`browser/shots/r16/zoom/<id>.{after,reference}.png`）——
> 当时只按需拍这几条，其余 7 条**没有截图、不假装有**。要重拍：
> `node tools/render-verify/browser/r16-shot-zoom.mjs <id...>`。
> 复现整套（约 4 分钟）：`python tools/render-verify/gen/round16_editor_reported.py` →
> `node tools/render-verify/browser/run-r16-browser.mjs` → `node tools/render-verify/browser/summarize-r16.mjs`。
> 逐条原话与源码在 `tools/render-verify/gen/round16_editor_reported.py` 的 `CASES` 里，**原样照录、未改一个字**。

**第二十四轮补：把验收搬回您自己那篇文章（#38），逐条复验「症状现在还在不在」。**
做法是把**修复前的编辑器**真的跑一遍（`HEAD` 版前端单独构建一份，在同一个浏览器里整包替换），
同一篇文章、同一个窗口宽度、同一把尺子，量三列：**改前 / 改后 / 后端产物**。
结论：**8 条批注里 5 条的症状在 #38 上确实消失（第 4、7、8、9、11 条），4 条仍在（第 1、5、6、10 条），
第 2 条是上游、第 3 条三列本来就一致**。

| # | 您说的 | 量的是什么 | 改前 | 改后 | 后端产物 | #38 上 |
|---|---|---|---|---|---|---|
| 1 | 缺少边框 | 卡片边框 / 圆角 / 胶囊画法 | `0px none` / `0` / `inline-block` | 与改前相同 | `1px solid` / `12` / `flex` | ❌ 仍在 |
| 2 | 原项目就没有正确显示 | 真表单元素数 | `0` | `0` | `0` | 上游 |
| 3 | 头像没有渲染完整 | 头像盒 / 裁切 / 圆角 / 原图尺寸 | `60×60` / `cover` / `50%` / `1080×784` | 相同 | 逐项相同 | 三列一致（源图横版） |
| 4 | 缺少底色 | 卡片高 / 包裹段下间距 / 首行位移 | **`145.19` / `17.25` / `45.25`** | **`127.94` / `0` / `28`** | **`127.94` / — / `28`** | ✅ 消失 |
| 5 | 原项目就没有正确显示 | 徽标盒 / 圆角 / 可见文字 | `24×24` / `50%` / **`72`** | 相同（`72`） | 相同 / **`78`** | ❌ 仍在 |
| 6 | 没有原项目的好看 | 两列宽度 / 字数文案 | **`[321,321]`** / 「共 `2865` 字」 | 相同 | **`[552,90]`** / 「共 `0` 字」 | ❌ 仍在（「好看」为主观项，只列客观差异） |
| 7 | 每一项的前边缺少列表符号 | 圆点盒 / 可见圆点数 | **`0×21`** / **`0`** | **`8×8`** / **`6`** | `8×8` ×6 | ✅ 消失 |
| 8 | 每一项的前边都多了一条竖线 | 方框盒 | **`4×25`** | **`20×20`** | `20×20` | ✅ 消失 |
| 9 | 行间距太大、整个表太高 | 表高 / 逐行高 | **`421.78`** | **`347.09`** | **`347.09`**（逐行相同） | ✅ 消失 |
| 10 | 原项目就没有正确显示 | 6×6 圆点个数 | `0` | `0` | `3` | ❌ 仍在 |
| 11 | 每一步的边框没有加圆角 | 表格 `border-collapse`/`spacing`/`min-width` | `collapse` / `2px` / `125px` | **`separate` / `12px 0` / `600px`** | 相同 | ✅ 消失 |

**为什么第 1、5、6、10 条「还在」——不是没修好，是那篇文章的正文停在修复前。**
修复做的事是「把渲染服务写下的排版声明，在编辑器里保住」。查了 #38 的存库正文：
- 第 7、8、11 条那几处，**声明一直写在正文里**（8×8 的圆点、20×20 的方框、表格的 `border-spacing: 12px 0`），
  是编辑器原来在丢、现在不丢了 —— 所以症状消失。
- 第 1、5、6、10 条那几处，**正文里从来就没有这些声明**：changelog 那段外面只有
  `<section style="margin-bottom: 14px">`（**没有边框、没有圆角、没有底色**）；
  DA01 那张表全文 **一个 `data-colwidth` 都没有**（所以没有「窄列」可还原）；
  infographic 那段**只剩一行标签、没有条目行**（所以没有圆点）；
  audience-fit 那句里上游把第三列**直接并进了正文文字**（`…可直接复制|高` 就是原文）。
  → 编辑器没有丢它，也没有东西可补。**要在这几条上看到变化，只能把文章重新渲染一遍**；
  而 #38 没有留下 Markdown 源文（`contentMarkdown` 为空），**无法重渲染**。
- 验证这个解释：把这几条的**新鲜产物**灌进同一个编辑器，量出来**与产物逐项相同**
  （边框 `1px solid`/圆角 `12`、列宽 `[552,90]`、圆点 `3` 个、可见文字 `78`）
  ⇒ 修复本身是有效的，差别只在「存库那份正文里有没有可修的东西」。

**本轮回归**：七套数字与第廿一轮快照重跑对照，**唯一差异是新增的一个自算字段**（用来钉死「差异条目 152 条」的计数口径），
去掉后两份 JSON 逐字节相同；真实界面同宽终验仍 **0/70**。**本轮未改任何产品代码、未提交、未推送。**

**第二十五轮补：把上面那句「停在修复前」拆开——4 条里到底哪几条是保存时丢的。**
上面第二十四轮只说了「正文里没有这些声明」，**没回答「当初是保存的时候丢的，还是渲染出来的那一刻就没有」**。
这一轮用**同题对照**补上：同一份输入，分别喂给**修复前的编辑器**和**修复后的编辑器**，各逼出一次保存，逐字节比。

| # | 您说的 | 定性 | 证据（同题对照，改前出口 vs 改后出口） |
|---|---|---|---|
| 6 | 列宽没有还原 | **旧编辑器在保存那一刻丢的** | 改前出口 `<colgroup><col style="min-width: 25px;"><col style="min-width: 25px;"></colgroup>`；改后出口 `<colgroup><col style="min-width: 25px;"><col style="width: 90px;"></colgroup>`。**旧编辑器确实会把窄列拍平**，拍平后的指纹与 #38 存库正文**一模一样** |
| 1 | changelog 缺少边框 | **正文停在更早的渲染版本**（不是保存丢的） | 同一份输入喂**改前**编辑器，保存出口里 `border-radius:12px` / `rgb(226,232,240)` / `1px solid` 的**计数与改后完全相同**（1 / 1 / 2，section 7 个）——**旧编辑器不丢这个容器**，所以 #38 缺它不能赖保存 |
| 5 | audience-fit | 同上（正文停在更早的渲染版本） | #38 存的片段与今天的产物**连文字都不一样**：存的是 `技术团队\|结构严谨、代码块清晰、API 文档可直接复制`，今天同位置是另一段文案 |
| 10 | infographic | 同上（正文停在更早的渲染版本） | 同上：#38 存的是 `共 2865 字`，今天同位置是 `共 0 字`；徽标数 8 vs 1 |

> ⚠️ **更正第二十四轮的一处说法**：那一轮写过「第 1、5、6、10 条正文里从来就没有这些声明」，
> 其中**第 6 条要收窄**——正文里确实留下了一个 `<colgroup>`，只是两列的宽度都被拍成了 `min-width: 25px`。
> 也就是说**第 6 条不是「没有可还原的东西」，而是可还原的东西被旧编辑器拍平了**。

**同一轮还答了您那句「我在编辑器里看到的样子，保存之后还在不在」**（方法与逐条对照见
`docs/dev/known-issues-handoff.md` §3.26①）：

| 您把内容弄进编辑器的方式 | 探针逐条相同 | 整段画面 | 结论 |
|---|---|---|---|
| **打开文章**（您的真实路径） | **11 / 11** | 逐字节相同 | ✅ **保存前后一模一样** |
| 粘贴 | **11 / 11** | 逐字节相同 | ✅ **保存前后一模一样** |
| 手打 | 8 / 11（3 条量不到，如实记空） | 3 处不同 | 差异**发生在「打开时的解析」，不在保存**；保存出口重灌后 **10 / 10 相同** |

手打那 3 处的真相：**只有「用半角空格或制表符在段首手工缩进」这一种写法会掉，而且是打开时就掉、不是保存掉**；
用 `&nbsp;`、全角空格、零宽空格都不会掉。**为什么会掉**：编辑器打开正文时用的解析方式默认**会合并连续空格**。
**候选修法评估后没有采纳**——打开「保留全部空白」虽然能救回段首空格，但**会改掉整篇文章的块结构**
（顶层块 14 → 15、每个块的位置整体位移、有的块高从 280.69 变成 87.75），
等于为了一个空格去动整篇排版。**这一条需要您拍板**（要不要冒这个险），详见 §3.26①。

**第二十六轮补：这一条已经修好了，而且没有付上面那笔代价。**
没有被采用的只是「打开保留全部空白」那把大锤；这一轮改用**只作用于段首的定点替换**——
把段首那一小段空白换成不会被合并的 `&nbsp;`，段中间、段尾、`pre`/`code`/`svg` 里的空白**一个字都不碰**。
判据在动手之前就写死了两条，跑完两条都过：

| 判据 | 改前 | 改后 |
|---|---|---|
| 段首空白**还在不在** | **1 / 7** | **7 / 7** ✅ |
| **79 个最小写法样例**的块结构 / 位置 / 高度**逐条不变** | — | **0 条不同** ✅（顶层块 203、`section` 338 等四项合计改前改后完全一样） |

> 代价只有一处，是**实测值不是估计**：**用制表符**做段首缩进时，缩进宽度从 **26.89px 变成 3.38px**。
> 「半角空格缩进」的视觉宽度**一分不变**（半角空格与 `&nbsp;` 都是 3.38px）。
> ~~接不接受这处变化，请您说一句~~ → **第二十七轮已把这处代价收掉，不需要您拍板了**（见下面「第二十七轮补」）。
> 另：段尾的 2 个空格在 HTML 里本来就不产生可见效果，所以**没有顺手改**——本轮修的只有您能看见的段首缩进。

**同一轮还把「窄列被拍平」从「一次对照实验」升级成了一道会自动报警的闸**：
新增的 `tools/render-verify/browser/r26-table-colwidth-exit.mjs` 会检查**保存出口**里每一列的宽度声明
是否与入口一致，并自带一个反例自检（拿旧编辑器的出口去喂它，**必须判失败**——实测确实判失败）。
闸不过就**退出码非 0**，以后谁把它改回去都会被当场抓住。
顺带量到一个此前没人查过的细节：`data-colwidth` **只是入口写法，出口不保留它**；
真正保住列宽的是出口里的 `colwidth="90"` 属性与 `<col style="width: 90px;">`。

**第二十七轮补：上面那处代价**（用制表符做段首缩进时宽度变窄）**已经收掉了，不用您再拍板。**
做法分两步，先量后改：
① **先量这种写法到底有多少**——把「段首用 `\t` 缩进」在**渲染产物 249 个**、**全部产物文件 403 个**、
**库存文章 38 篇**里各数一遍，结果是 **0 处**；
② **再改成等宽**——制表符不再换成「1 个 `&nbsp;`」，而是按制表位**展开成同样宽的一串**（1 个 `\t` = 8 个 `&nbsp;`）。
实测缩进位置 **224.89 → 224.88px**（差 0.01px），「半角空格缩进」逐值不变；
动手前写死的三条判据全过：**段首空白存活 7 / 7**、**79 个样例块结构/几何 0 条不同**、**无新增视觉差异**。

**同一轮还补齐了「内容是怎么进编辑器的」这一维**（此前只量过「打开文章」那一条路）：

| 怎么进去的 | 改前：保存后再打开 | 改后：保存后再打开 |
|---|---|---|
| **打开文章** | 缩进丢 | **6.72px / 26.88px 保住** ✅ |
| **粘贴纯文本**（从记事本、代码编辑器复制） | 缩进丢 | **保住** ✅（顺带修好的） |
| **手打**（在段首敲空格） | 缩进丢 | **保住** ✅（顺带修好的） |
| **粘贴网页 HTML** | 缩进丢 | 缩进丢（**与改前逐字相同**，不是我们这轮弄坏的） |

最后一行是唯一没修的：它是**剪贴板里 HTML 的空白折叠**，属于编辑器底层的既有行为。
要修也做得到（技术上是一行，可行性已实测），但它改的是**「粘贴」这条路的全局行为**，
**要不要动请您说一句**（已列进 §3.27③ 待拍板表的第 9 条）。

**存量影响面（只读扫描，判据只看正文一个字段，您也可以自己重跑）**：库里未删除文章 **38 篇**，
命中「列宽被拍平」的 **2 篇（5.3%，#24、#38）**，命中「changelog 缺容器 / infographic 只剩标签 / audience 残留竖线」的**各 1 篇（#38）**，
**命中任意一条的合计 2 篇（5.3%）**。
**但「把这两篇重新渲染一遍」用现有功能做不到**——现有的重渲染要求「文章留有 Markdown 源文」，
而**恰好这两篇都没有**（#24 引擎是 MARKFLOW 但源文为空、#38 是 PROMPT 且源文为空）。
要不要动这两篇、用哪种方式动，**等您定**（见 §3.26③）。

**第二十八轮回归**：**本轮没有改任何一行产品代码**，所以按复现手册把整条链重跑一遍核对即可——
每一步退出码都是 0：79 样例 `pass 70 / na 9 / fail 0 / unverified 0`、17 组合 `pass 17`、
10 条替代写法 `pass 10`、76 组 `layout-*` `na 76`、交叉验证 `18/18`、
终稿表 `backendOk 25 / editorPass 25 / dangling 0`、第十六轮本体 `diffEntries 152`、
真实界面同宽终验 **0/70（退出码 0）**——**与第廿一轮快照一致**（四份汇总**逐字节相同**，
`r16_summary.json` 只多那个自算字段 `diffEntries`；`r16_result.json` 11 条里只有 1 条不同、
恰好 4 个叶子值，仍是第二十三轮那个 `<p style="margin: 0px;">`）。
#38 的十一条四列表**本轮不重跑**：本轮没改产品代码，「产品代码变了就重跑」的前提不成立；
作为**同强度的替代**，真实界面同宽终验刚刚在同一篇文章 #38 上跑过同一组 11 条 × 70 组探针，**0/70 有差**。
8081 上发的仍是 `ArticleEditorView-BsvRE3g6.js`（与上一轮**同一个 hash**，本轮没改产品代码，本就该不变），
从 8081 **取回**的 chunk 与 `webui/dist`、`target/classes/static` 两份**sha256 逐字节相同**；
`/articles/43` 公式 5/5、`/articles/44` 轮播 3/3。
数据库：`#38` 保持 `revision=15` / `updatedAt=2026-09-13 21:22:55.655425`，`#24` 保持 `revision=3` /
`updatedAt=2026-09-12 22:13:16.144323`，**逐字未变**，写请求全部被拦下。**本轮未提交、未推送，改动留在工作树。**

**第二十七轮回归**：产品代码又动了（段首空白那处修复的**等宽展开**），所以**全文重验**：
① 把 #38 那十一条四列表**改前 / 改后两列各复跑一遍**，与第二十四轮的表**逐值对账**——
**每一列各 125 个值全同，0 处差异**（第 4、7、8、9、11 条的卡片高、圆点盒、方框盒、表盒高与逐行高、
表级声明都与原表**逐项对上**）；
② 七套回归照跑，仍是 79 样例 `pass 70 / na 9 / fail 0`、17 组合 `pass 17`、10 条替代写法 `pass 10`、
76 组 `layout-*` `na 76`、交叉验证 `18/18`、终稿表 `backendOk 25 / editorPass 25 / dangling 0`、
第十六轮本体 `diffEntries 152`、真实界面同宽终验 **0/70（退出码 0）**——
**与第廿一轮快照一致**（四份汇总逐字节相同，`r16_summary.json` 只多那个自算字段）；
③ 那条**已知的唯一 1 处快照差异**再次对上（第 4 条金句卡的 `<p style="margin: 0px;">`，出自第二十三轮，与本轮无关）。
另确认 **8081 上跑的确实是含本轮改动的那份前端**（编辑器 chunk = `ArticleEditorView-BsvRE3g6.js`，
旧包是 `04PITbhP`；服务出的 chunk 里查得到本轮的制表位展开代码），
`/articles/43` 公式与 `/articles/44` 轮播均正常；每次跑完都回读数据库，`revision`/`updatedAt` **逐字未变**。
**本轮未提交、未推送，改动留在工作树。**

**第二十六轮回归**：**改了产品代码**（`preserveLeadingWhitespace()`，接在编辑器打开正文的两处），
所以整条链**本轮重新跑了一遍**：79 样例 `pass 70 / na 9 / fail 0`、17 组合 `pass 17`、
10 条替代写法 `pass 10`、76 组 `layout-*` `na 76`、交叉验证 `18/18`、
终稿表 `backendOk 25 / editorPass 25 / dangling 0`、第十六轮本体 `152` 条差异、
真实界面同宽终验 **0/70（退出码 0）**——**逐项与第廿一轮快照一致**（四份汇总逐字节相同，
`r16_summary.json` 只多那个自算字段 `diffEntries`）。
另外确认 **8081 上跑的确实是含本次改动的那份前端**（编辑器 chunk = `ArticleEditorView-04PITbhP.js`，
旧包是 `BFvBiRez`），`/articles/43` 公式与 `/articles/44` 轮播均正常；每次跑完都回读数据库，
`revision`/`updatedAt` **逐字未变**。**本轮未提交、未推送，改动留在工作树。**

**第二十五轮回归**：**未改一行产品代码**（上面的差异归因到「打开时的空白合并」和「#38 自身的旧数据」，
都不是保存出口缺陷，所以没有为改而改）；探针侧与第廿一轮快照重跑对照**无漂移**
（四份汇总逐字节一致，`r16_summary.json` 只多一个自算字段 `diffEntries`）；
真实界面同宽终验仍 **0/70**；三次保存出口实验每次都回读数据库核对，`revision`/`updatedAt` **逐字未变**。
**本轮未提交、未推送。**

**第 10 条的额外取证**（它靠「换写法再打」才复现，所以证据单独一支）：
`r16/infographic_variants.json`（10 种写法）、`r16/r10_by_design.json`（4 组件 × 3 写法）、
`r16/r10_blockscalar.json`（11 组块标量边界），生成脚本
`tools/render-verify/gen/round18_infographic_variants.py` 与 `tools/render-verify/gen/round20_field_block_scalar.py`。

**已修的五类根因**（都只改编辑器，不动渲染服务）：

1. **表格的排版声明没进编辑器实时画面**（第 11 条）。渲染结果把卡片间距、圆角写在表格上，
   编辑器只把其中一句 `min-width` 画了出来 —— 于是圆角没了、卡片挤在一起。现在整段声明原样贴回。
2. **空的小方块图形被压扁**（第 7、8 条，以及第 5、10 条的徽标）。编辑器会把这类小图形额外包进一层段落，
   包完之后它的宽高就不生效了 —— 8×8 的圆点整个消失，20×20 的方框塌成一条竖线。现在补齐了那层段落的排版上下文。
3. **光标占位被当成图片排版**（第 7、8 条的「行距变大」）。编辑器给空位置插的零宽占位图没有基础样式，
   落进了「图片一律块级、上下各留 18px」的规则里。现在单独归零。
4. **窄列宽度丢失**（第 6 条）。渲染服务写的 `data-colwidth` 编辑器认不出来，于是左右两列被平分成一样宽
   （344.5 / 344.5），而原本应该是 637 / 90。现在能读进来了。
5. **凭空多出来的段落间距**（第 4 条，第二十三轮）。卡片里那个大引号是**绝对定位的装饰元素**，
   在产品里不占任何高度；编辑器把它包进一层段落，同时又给这层**根本不存在的段落**补了
   1.15em 下间距 —— 卡片于是凭空高出 17px。现在这类「整层内容都脱离文档流」的包裹段落不给段落间距，
   实测高度 145 → 128，与产物 128 相同。

**回归**：改完之后把已有的全部套件重跑了一遍，**第十六~二十轮每轮再各重跑一遍**，
**第二十三轮再重跑一遍**（含第 4 条的修复），数字与改动前**逐项一致、无上游漂移**——
**79 个样例 70 通过 / 9 不适用 / 0 失败 / 0 未验**、
**17 个组合**（渲染服务 8 通过 / 8 不支持嵌套 / 1 静默丢失；编辑器 17/17 通过）、
**10 条替代写法 10/10**、**76 组版式写法 76 全部确认引擎未实现**、
**独立交叉验证 18/18 确认、0 存疑**（对照组 3 条 0 误判）、
**组件终稿表：总数 63 / 两条路径都通过 25 / 等上游 38 / 悬空 0**、
**编辑器往返差异条目 152 条**（改前 174 条；这个数的口径第二十三轮已钉死为
「所有探针组逐属性差异注记的总条数、不截断」，见 `known-issues-handoff.md` §3.23③）。

**如实说明三件事**：

- **有一条**我们**已经查清、但还没有解决**的路：如果您不是「打开文章」，
  而是把别处的内容**粘贴**进编辑器，那么这些组件的**外层容器会被丢掉**（实测 11 条用例合计 51 层 → 32 层，
  金句卡连自己那层都没了 —— 那层上挂着底色和圆角）。**这不影响您这次报的 11 条**：
  我们查过**您自己那篇文章的存库正文**，那些外层容器**一层都没少**，说明您走的是「打开文章」这条路，
  不是粘贴（证据与判据见 `docs/dev/known-issues-handoff.md` §3.23①）。
  粘贴这条路**以前没有当功能做过**，现在如实记下来：将来若要做「从别处粘组件进来」，这是必须先解决的前置问题。

- 编辑器侧「逐属性完全一致」的**机器口径**仍然是 11 条里 1 条。这个口径里有两类**已知的假差异**
  （两侧面板宽度差 38 像素造成的宽度差；编辑器多包一层段落造成的取样错位），
  我们没有为了让它变好看去改判定规则 —— 明细和自证写在 `known-issues-handoff.md` §3.20⑤。
- 第 10 条的定性**在第二十轮又被自己推翻了一次**，这次是往回收。原先按本项目自己的规矩
  （`upstream-issues.md` §R4-附）记为「不立项」——「某个写法在上游不渲染」这个结论，必须先拿到可复现的证据，
  **测不出来就不能写成上游有问题**。当时只试了您给的那一种写法（结果正常），所以停在「未能复现」。
  第十八轮把 `body` 的写法换了 10 种重打真实接口，复现出了退化（换成条目列表、或缩进少一个 `|` → 三条正文整块消失且不报错），
  立为 **§R10**，当时的定性是「渲染缺陷」。
  第二十轮我们又反过来查自己：**`|` 本来就是块标量的必需语法，缺了它按字面处理是不是本来就对？**
  三组实测（4 个组件表现完全一致、`|` 在 `label`/`title` 上也照样生效、带冒号的正文行不会被误当字段）
  都指向同一结论：**这是引擎的字段级统一约定，少写 `|` 属于写法不合约定，不是 infographic 的渲染缺陷**。
  于是 **§R10 被降级为「官方文档没写这条规则 + 静默失败」**（P3），**不再主张上游渲染有 bug**。
  这条改判对您也有用：**您给过来的那种写法（`body: |`）是完全正确的**，按它写不会有问题。
  **唯一还没对上的**：您当时用的到底是哪一种写法——您给过来的那段本身是好的，所以还不能说您看到的就是这一条。
  如果您能截一张出错时的图或把原文贴回来，我们就能把它对上号。

---

## 七、想自己重跑一遍？

全套命令、前置条件、每个套件的预期数字，都在 **`docs/dev/render-verification.md`**。
**探针脚本从第十四轮起已经纳入版本控制**（`tools/render-verify/`，含组件清单与引擎包存档），
所以「照着跑」不再需要先向别人要脚本——只需要自备三样环境：渲染令牌、`webui/node_modules`、`.env`，
手册第零节与第一节写清了怎么补。跑出来的产物仍落在 `target/probe/`（不入库，每次可重算）。

逐轮的原始数字与判定细节在 `docs/dev/known-issues-handoff.md` §3.9–§3.20，
渲染引擎侧的问题清单在 `docs/dev/upstream-issues.md`（第十六轮新增的 §R5–§R9、第十八轮新增的 §R10，
都在「一·续」节；其中 §R5–§R9 是 **5 条渲染缺陷**，§R10 是第二十轮降级后的
**「文档未覆盖 + 静默失败」（P3）**，不主张上游渲染有 bug）。
